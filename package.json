{
  "name": "farm",
  "version": "0.0.0",
  "private": true,
  "description": "Farm OS monorepo — @amrebeid/ui design system (packages/ui) + product docs (docs/)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@storybook/react-vite": "^10.5.10",
    "jsdom": "^29.1.1",
    "react": "19.2.7",
    "react-dom": "19.2.7",
    "storybook": "^10.4.6"
  },
  "overrides": {
    "react": "$react",
    "react-dom": "$react-dom",
    "postcss": "^8.5.23",
    "nanoid": "3.3.18",
    "@changesets/parse": {
      "js-yaml": "4.3.1"
    },
    "@eslint/eslintrc": {
      "js-yaml": "4.3.1"
    },
    "read-yaml-file": {
      "js-yaml": "3.15.1"
    },
    "exceljs": {
      "uuid": "11.1.1"
    },
    "tsup": {
      "esbuild": "0.27.2"
    },
    "@types/node": "^22"
  },
  "comment-overrides-tsup-esbuild": "GHSA-g7r4-m6w7-qqqr affects esbuild 0.27.3-0.28.0 (arbitrary file read via the esbuild dev server on Windows). tsup 8.5.1, the latest, declares esbuild ^0.27.0, so no in-range version above the advisory exists; 0.27.2 is the highest in-range version that predates it and keeps the tree valid (no semver violation). Drop this override once tsup widens its range to ^0.28.1 or later."
}
