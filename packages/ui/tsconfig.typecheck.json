{
  "//": "Typecheck-only config. The react/react-dom paths pin a single @types identity so hoisted deps (e.g. recharts) typecheck cleanly. Kept OUT of the base tsconfig.json so Storybook/Vite runtime module resolution is not redirected to @types (which has no runtime entry points).",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "react": ["./node_modules/@types/react"],
      "react/*": ["./node_modules/@types/react/*"],
      "react-dom": ["./node_modules/@types/react-dom"],
      "react-dom/*": ["./node_modules/@types/react-dom/*"]
    }
  }
}
