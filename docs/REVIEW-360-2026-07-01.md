# REVIEW-360 — Farm OS full-product review (2026-07-01)

*Five independent review lanes (architecture, DB/security/engine, product plan, frontend/UX, external market research) run against `origin/main` `6184961` (post the 32-PR live-deploy). Read-only; this doc is the durable record. Owner: Amr Ebeid.*

## Scorecard

| Dimension | Grade | Headline |
|---|---|---|
| DB / Security / Engine | **A−** | 55/55 FORCE RLS, catalog-level CI invariants, engine masked-shortage-free. Docked for accounting-kernel completeness gaps. |
| Architecture | **B+** | Coherent core (one way to do most things); debt at the seams. |
| Frontend / UX | **B+** | Arabic-Indic + RTL genuinely enforced; a11y above market. |
| Field-mobile readiness | **C+** | `/m` execute flow excellent; offline support = zero. |
| Plan coherence | **C+** | Product outran the plan; wedge pillars empty; no current-status doc (fixed by `STATUS.md`). |

**Core verdict:** the codebase is ahead of the plan. The wedge's three value pillars are all still empty — real registry not imported, budget gate display-only (#157), chart of accounts empty — while feature depth shipped. Binding constraint = owner decisions + real data, not code.

---

## 1. DB / Security / Engine findings (grade A−)

| # | Sev | Finding | Fix (one PR) |
|---|---|---|---|
| D1 | **P1** | Custody↔GL completeness gated on a magic Arabic string: journal posts only when `p_movement_type = 'استلام عهدة من المالك'` exactly (`20260701220000:458-460`); `custody_movements.movement_type` is free text, no CHECK → cash-ins with any other label create custody movements with **no GL entry**; trial balance silently diverges. | movement_type CHECK vocabulary; post a journal for every money-bearing type or loudly reject; pgTAP: no money-bearing movement without `journal_entry_id`. |
| D2 | P2 | `audit_log.audit_read` is a second `authorize()`-class re-emit footgun with **no completeness pin** (re-emitted 4×; nothing like tests/97 pins the sensitive-entity blocklist). A stale re-emit would silently expose payroll/finance audit rows to all org members. | tests/97-style pgTAP asserting every sensitive entity_type appears in the policy expression. |
| D3 | P2 | Journal reversals designed but unreachable: `status='reversed'`+`reversal_of` exist, error copy says "post a reversal first", but **no RPC posts one** and DML is revoked. Idempotency key `(org, source_type, source_id)` also silently no-ops a legitimate re-post after a reversal. | `fn_reverse_journal_entry` (owner/accountant) + sequence-suffix semantics for re-posts. |
| D4 | P2 | No custody balance floor: `fn_record_custody_movement` never checks balance ≥ amount_out — custody can go arbitrarily negative silently (inventory has a floor; cash doesn't). | Floor check + explicit overdraft flag + pgTAP. |
| D5 | P2 | Stale sibling `fn_add_plan_operation` is target-blind: dedup key lacks `target_type/target_id` (`20260622000062`) vs the multi RPC (`20260701340000`) → a palm-targeted op can swallow an intended plan-scope demand ("deduped"), engine never sees it. | Re-emit with target-aware dedup or delegate to the multi RPC. |
| D6 | P3 | FORCE RLS pinned only for the static 0028 list — a new table that ENABLEs but forgets FORCE passes CI. | One dynamic invariant: `count(relrowsecurity and not relforcerowsecurity) = 0`. |
| D7 | P3 | 16-positional-arg `fn_add_plan_operation_multi`: mitigated (named params, clean DROP lineage) but `p_soil_moisture_reading` is unvalidated free text adjacent to `p_note` — silent-swap risk. | **Freeze the signature**; future params via `p_options jsonb`; validate soil reading. |
| D8 | P3 | `fn_execute_operation` legacy NULL-target path writes an unvalidated uuid into `event_locations.sector_id` (`20260701390000:213-215`). | Validate against `sectors`, null-out with note on mismatch. |
| D9 | P3 | Hygiene: one merged migration edited post-merge (comment-only, `20260701240000`); test-file prefix collisions (six `112_`...); tests/22 comment misattributes a migration. | Policy: never edit merged migrations; timestamped test prefixes; fix comments. |
| D10 | P3 | Engine excludes `blocked` op demand by design — guarded only by a comment. | ADR note or pgTAP doc-pin for the semantics. |

Verified clean (probes in review record): authorize() 18-perm union complete; RLS/FORCE 55/55; no views; RPC surface catalog-guarded (tests/22 INV-1/2/5); post_movement/bin_rebuild internal-only; receipt claim-first + over-receipt cap; engine bucket-origin clamp + stale-PO guard; accounting kernel balanced-entry trigger + idempotent journals; grant hygiene swept.

## 2. Architecture findings (grade B+)

| # | Sev | Finding | Fix |
|---|---|---|---|
| A1 | P1 | `lib/database.types.ext.ts` = 875-line hand-forked type augmentation (5-deep nested wrappers; 7 hand-typed RPC blocks unchecked vs SQL). | Regenerate types from prod; delete satisfied augmentations; add regen ritual/CI check. |
| A2 | P1 | `packages/ui/dist/` committed (force-added past .gitignore) and is what the app builds against — **no drift guard**; a src change without rebuilt dist ships nothing, CI stays green. | CI step: build ui workspace + `git diff --exit-code packages/ui/dist`. |
| A3 | P2 | Zero component tests (49 client components; vitest scoped to `lib/**`) and the 1 Playwright wedge-loop spec runs in no workflow. | jsdom vitest project for top-3 forms; decide e2e-in-CI or document as manual release gate. |
| A4 | P2 | 3 filenames with literal spaces (`lib/linked work context.ts`, `components/linked work sections.tsx`, `components/print button.tsx`) + case-convention split. | Rename + filename-case lint. |
| A5 | P2 | Arabic label maps duplicated outside `lib/labels.ts` (`EXPENSE_KIND_AR` ×3, `PAYMENT_STATUS_AR` ×2). | Hoist to labels.ts + test. |
| A6 | P2 | 8 Supabase reads discard `error` (pest-scouting actions, weather-thresholds, auth) — transient DB errors read as "not found". | Destructure + handle at the 8 sites. |
| A7 | P3 | Table framework: 3 wrappers (Simple/Filterable/Master) — principled ladder but MasterTable has only 2 users. | Converge/document "which table when" in SPEC-0017. |
| A8 | P3 | Dead components `AddSupplier.tsx`, `WhyButton.tsx` (0 importers). | Delete. |
| A9 | P3 | Sequential dependent awaits on heaviest pages (plans/[planId] ×3, pva, dashboards). | Merge lookups per page when next touched. |
| A10 | P3 | Flat `lib/` (60+) & `components/` (50) + >500-line pages. | Forward rule: `lib/<module>/` for new helpers; extract sections past ~400 lines. |

Keep untouched: supabase client trio, requireMembership 45/45 pages, RPC-first writes, the bespoke CI guards, recharts containment, page-help drift-guards, docs/catalog system.

## 3. Frontend / UX findings + recommendations (grade B+; field-mobile C+)

**The frontend work-list (each one PR):**

| # | Sev | Finding | Fix |
|---|---|---|---|
| F1 | **HIGH** | **Offline tolerance doesn't exist** (zero SW/IndexedDB/outbox; `ExecuteForm` comment admits it; PWA manifest has no icons → not installable). Field workers with no signal get a dead app. | PR-1: ExecuteForm offline **outbox** — persist failed `executeOperation` payloads (localStorage), Arabic "سيُعاد الإرسال عند عودة الاتصال" banner on `/m`, replay on reconnect (RPC already idempotent → safe). PR-2: PWA icons + offline fallback page. |
| F2 | **HIGH** | `Number('')→0` fabricated-zero write path in `OperationBuilder.tsx:198,215-216` (default material row + un-deletable sole row); server permits qty 0 (`plans/[planId]/actions.ts:319,338`). Violates never-fabricate at the DB layer. | Server: reject `<= 0` qty/count/days; client: filter blank material rows, allow deleting sole row. |
| F3 | **HIGH** | 8 forms strand the spinner forever on network failure (AddExpense, AddSupplier, PlanCreateForm, PersonCreateForm, LaborLogForm, StructureForm, RecordActivity, WeatherThresholdsForm) — no try/catch/finally. | Extract a `useSubmit` helper (Arabic retryable error + `finally setPending(false)`), apply to all 8. |
| F4 | MED-HIGH | No bidi isolation: codes/phones (`PR-2024-001`, `+20…`) visually reorder inside RTL at ~10 sites (purchase-requests, accounting, croquis, suppliers). | 5-line `<Code>` (`<bdi dir="ltr">`) component; apply at the sites. |
| F5 | MED | `/m` feed query unbounded (no `.limit()`/date window) — grows every season, on the flaky-network surface. | Server-side: executable statuses + overdue+14-day window + "عرض المزيد". |
| F6 | MED | **Storekeepers have no phone surface** — `/m` role-gated away; they land on a chart-heavy desktop dashboard; their daily action (receive stock) is desktop-framed. | `/m/receive`: card-list of pending receipts reusing ReceiveForm + the `/m` pattern. |
| F7 | MED | Validation is banner-only; DS field-level error affordances (`FormRow`/`Field` aria-invalid) are dead code; required-asterisk aria-hidden. | Thread field-keyed errors into FormRow for ExecuteForm/LaborLogForm/ReceiveForm; `aria-required` pass-through. |
| F8 | MED | Numeric keyboards inconsistent on phones: `inputMode="numeric"` on decimal fields (hides decimal separator on many Android keypads); money fields have no inputMode. | Normalize `inputMode="decimal"` on all money/qty fields. |
| F9 | MED | Images: raw full-res `<img>` (MediaGallery renders storage originals at ~112px; no lazy/CLS). | `next/image` fill+sizes + Supabase remotePatterns. |
| F10 | LOW-MED | Polish batch: KPI delta colour-only (WCAG 1.4.1) → bake arrow+text into KpiCard; `/dashboard` router double-pays auth → route by JWT claim; merge 2 people lookups in plans/[planId]; breadcrumbs only on farm module → extend to plan/PR/supplier detail; 1 physical-prop leak `mr-8`→`ms-8` (OperationBuilder:447); 3 actions hand-roll SQLSTATE maps → `toArabicError`. | One themed PR. |
| F11 | LOW | Consistency (from boundary audit): two KPI primitives (`KpiCard` ×24 pages vs `Stat` ×2) → collapse to one; no shared `PageHeader` (h1 size drift text-2xl vs text-xl across pages) → add DS PageHeader; 360 attribute layout split (DescriptionList-in-Card vs hand-rolled grid) → standardize on DescriptionList; single `(app)/error.tsx` for all 45 routes → add per-module error boundaries for the finance + `/m` groups at least. | One themed PR. |

**Frontend strengths to protect (don't "modernize"):** centralized `ar-EG` number/date discipline (zero Western-digit leaks — consider a lint banning `toLocaleString/toFixed` in app code to make it permanent); the `/m` execute flow as the phone-first template (44px targets, prefill-from-plan, idempotent RPC, REI/PHI banner); WAI-ARIA Tabs/overlays/palette; `Promise.all` fan-out + CI-guarded recharts code-split; role-shaped nav registry; honest empty-states/comments culture.

## 4. Plan / process findings (grade C+)

1. No current-status doc → **fixed: `STATUS.md`** (this review's companion).
2. Independent-review rule degraded to same-session self-review under autonomous mandates; it twice missed its own regressions until live execution. → Restore: no prod migration without a separate review session (even short).
3. Stage-P controls unmet while prod deploys continuously: no staging env (migrate-first = migrate-**prod**-first), no monitoring/alerting, no backup/PITR verification, no rollback drill. → Ops-hardening mini-spec.
4. Migration-ledger fragility: `apply_migration` auto-versioning required manual `schema_migrations` surgery twice. → Always pass explicit version; verify ledger before calling apply done (recorded in SESSION-BRIEF lesson).
5. Docs sprawl: 87 files, ~25 dated one-shots, 3 roadmaps, 2 decision docs + #505. → Archive pass (see drift-sweep doc when it lands) + STATUS.md as the only current doc.
6. Risk-register deltas for MASTER-PLAN §6: add single-owner bus factor; agent-velocity vs review capacity; ledger fragility; no staging/monitoring/DR; onboarding/training plan for Arabic-first field staff; `authorize()`/audit_read re-emit structural debt. Mark R1 partially closed, R2 mitigated+pinned, R6/R7 live-not-planned, R11 materialized.
7. Scope discipline: ~half the 2026-07-01 wave was compliance/agronomy depth ahead of real usage. → Feature freeze until Stage M (in STATUS.md).

## 5. Market research delta

Recorded separately in `MARKET-DELTA-2026-07-02.md` (ETA urgency, Traction Ag consolidation, Zr3i scale, Mazoon Soft, KSA white space, product opportunities incl. the pollination module).

## Recommended PR order

1. Money integrity: D1+D4 together, then D2 (cheap, high leverage), D3, D5.
2. Field honesty: F2 (fabricated zeros) → F3 (8 forms) → F1 (offline outbox) → F6 (storekeeper).
3. Hygiene batch: A1, A2, A4, A5, A6, D6.
4. Polish: F4/F5/F7–F11, A7–A10, D7–D10 as themed batches when touched.
