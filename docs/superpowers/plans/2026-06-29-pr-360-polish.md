# Purchase Request 360 Polish Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` or `superpowers:executing-plans` to implement this plan task-by-task. Keep the checklist current while working.

**Goal:** Polish the existing read-only/action Purchase Request 360 route at `/purchase-requests/[prId]` so it follows the dashboard/360 convention: KPI cards at the top and line rows linking into Item 360.

**Architecture:** Keep the existing PR detail route and actions. Add query-derived KPI cards from the already-read PR lines and add `href` on line rows. No migrations, no RPC changes, no approval/receipt logic changes, no package UI changes.

## Constraints

- Arabic RTL first.
- No fabricated financial data; null `est_cost` stays unknown.
- No migrations, RPC changes, service-role usage, or new writes.
- Do not change `PrActions` behavior.
- Do not change `packages/ui`.
- Do not import chart libraries.
- Validate with targeted lint, `npx tsc --noEmit`, `npx vitest run`, and `npm run build` from `apps/farm-os`.
- Update docs last.
- Do not merge, migrate, deploy, or apply database changes.

## Tasks

- [x] Update `apps/farm-os/app/(app)/purchase-requests/[prId]/page.tsx`.
  - [x] Import/use `KpiCard`.
  - [x] Add KPI cards: line count, estimated cost, received quantity when units match, remaining quantity/open lines.
  - [x] Link line rows to `/inventory/[itemId]`.
- [x] Update design spec/tracker/session brief last.
- [x] Validate:
  - [x] Targeted lint
  - [x] `npx tsc --noEmit`
  - [x] `npx vitest run`
  - [x] `npm run build`

## Non-Goals

- No approval-state rewrite.
- No receipt logic changes.
- No budget-gate math changes.
