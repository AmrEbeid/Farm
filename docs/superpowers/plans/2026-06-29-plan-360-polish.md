# Plan 360 Polish Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` or `superpowers:executing-plans` to implement this plan task-by-task. Keep the checklist current while working.

**Goal:** Polish the existing Plan 360 route at `/plans/[planId]` so it follows the same dashboard/360 convention: KPI cards near the top and valid styled links for quick actions.

**Architecture:** Keep the existing plan detail route, operations table, checks, and plan actions. Add query-derived KPI cards from already-read operations/checks, and replace nested `Link > Button` quick actions with styled links. No migrations, no RPC changes, no action behavior changes, no package UI changes.

## Constraints

- Arabic RTL first.
- No fabricated financial/farm data; KPIs must be query-derived.
- No migrations, RPC changes, service-role usage, or new writes.
- Do not change `OperationBuilder`, `PlanChecksRunner`, or plan action behavior.
- Do not change `packages/ui`.
- Do not import chart libraries.
- Validate with targeted lint, `npx tsc --noEmit`, `npx vitest run`, and `npm run build` from `apps/farm-os`.
- Update docs last.
- Do not merge, migrate, deploy, or apply database changes.

## Tasks

- [x] Update `apps/farm-os/app/(app)/plans/[planId]/page.tsx`.
  - [x] Add `KpiCard`.
  - [x] Add KPI cards: operation count, estimated cost, checks run, blocked checks.
  - [x] Replace quick-action `Link > Button` wrappers with styled links.
- [x] Update design spec/tracker/session brief last.
- [x] Validate:
  - [x] Targeted lint
  - [x] `npx tsc --noEmit`
  - [x] `npx vitest run`
  - [x] `npm run build`

## Non-Goals

- No check-running or operation-creation behavior changes.
- No budget/coverage math changes.
- No schema changes or migrations.
