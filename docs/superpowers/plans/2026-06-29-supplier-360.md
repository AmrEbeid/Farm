# Supplier 360 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` or `superpowers:executing-plans` to implement this plan task-by-task. Keep the checklist current while working.

**Goal:** Add a read-only Supplier 360 route at `/suppliers/[supplierId]` and link supplier list rows to it.

**Architecture:** Build an additive read-only Server Component from existing RLS-scoped supplier, preferred inventory item, PR line, expense, and inventory movement tables. No migrations, no RPCs, no writes, no package UI changes.

## Constraints

- Arabic RTL first.
- No fabricated supplier or financial data; all KPIs must be query-derived.
- No migrations, RPC changes, service-role usage, or writes.
- Do not change `packages/ui`.
- Do not import chart libraries.
- Validate with targeted lint, `npx tsc --noEmit`, `npx vitest run`, and `npm run build` from `apps/farm-os`.
- Update docs last.
- Do not merge, migrate, deploy, or apply database changes.

## Supplier 360 Contract

- Header/profile: name, phone, terms, lead time.
- KPI cards:
  - Preferred items.
  - Linked PR lines.
  - Recent expenses total.
  - Inventory movements.
- Tables:
  - Preferred inventory items, linking to item 360.
  - Linked PR lines, linking to PR 360.
  - Recent expenses.
  - Recent movements.

## Tasks

- [x] Create `apps/farm-os/app/(app)/suppliers/[supplierId]/page.tsx`.
- [x] Update `apps/farm-os/app/(app)/suppliers/page.tsx`.
  - Link supplier rows to `/suppliers/[supplierId]`.
- [x] Update `docs/superpowers/specs/2026-06-29-module-navigator-dashboards-360-design.md`.
  - Add supplier 360 follow-up note.
- [x] Review before validation.
  - Check for writes/RPCs, invalid nested anchors/buttons, fabricated placeholders, chart imports, and row-link behavior.
- [x] Validate.
  - [x] `npx eslint ...`
  - [x] `npx tsc --noEmit`
  - [x] `npx vitest run`
  - [x] `npm run build`
- [x] Update `docs/PROJECT-TRACKER.md` and `docs/SESSION-BRIEF.md` last.

## Non-Goals

- No supplier editing changes.
- No supplier scorecard math beyond query-derived counts/totals.
- No schema changes or migrations.
