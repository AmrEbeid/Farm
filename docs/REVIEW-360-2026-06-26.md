# Farm OS — Deep 360° Review (2026-06-26)

*Six parallel specialist reviewers (security/RLS, stock-coverage engine, frontend/UX/i18n, app
code-quality, financial/data-integrity, open-issue reconciliation) run after prod was synced to
migration **0066**. Each verified findings against current `main` + live prod; nothing re-reported
that 0049–0066 or merged app code already fixed.*

## Context
- Prod Supabase `veezkmytervjnpxcrbkw` is at **0066**, in sync with `main`. The 18-migration push
  (0049–0066) is live and verified; advisors show **no new** regressions (only pre-existing WARNs:
  intentional gated DEFINER RPCs, anon-exec helpers, leaked-password toggle).
- App (Vercel `farm-ui`) deploys on merge; last prod deploy succeeded 2026-06-26 13:34.
- Headline: **the substantive holes are closed.** Security posture is strong, the engine core is
  sound, the frontend/data-quality issues are largely fixed. What remains is a short, precise list.

## Verdict by dimension
| Dimension | Verdict |
|---|---|
| Security / RLS / authz | Strong. No CRITICAL/HIGH exploitable. One MED residual (palm_status_history) + LOW hardening. |
| Stock-coverage engine | Core sound (disjointness, atomicity, concurrency all verified). 2 narrow shortage-masks (#270 C1/C2) + a missing input CHECK. |
| Frontend / UX / i18n | Mostly fixed. 1 HIGH (landing fabricated KPIs) + 1 MED (offline error handling) — both fixed in this PR. |
| App code-quality | Excellent (lint/tsc clean, 110 tests pass, zero `as any`/`@ts-ignore`). Offline-handling gap (fixed) + 1 swallowed read error. |
| Financial / data-integrity | PR state machine, SoD, wage confidentiality sound. Budget guardrail not table-backed (HIGH, product). Grant-layer regression (MED). |
| Open issues | #306, #280 closed (verified fixed). #270 kept for C1/C2. Several closeable; research issues = backlog. |

## Findings & disposition

### Fixed in this PR (branch `docs/push-prep-0049-0066`)
| Sev | Finding | Fix |
|---|---|---|
| HIGH | Landing page `app/page.tsx:16-18` hardcodes farm KPIs (٥/٢٨/٤٬٣٨٠) — fabricated data | Removed the tiles (page is unauthenticated → no org context; real KPIs live behind auth) |
| MED | 6 mutation forms hang on a stuck spinner offline, no Arabic error (only ExecuteForm was hardened) | Added the `try/catch/finally` + retryable Arabic message to PalmStatusForm, ReceiveForm, PrActions, CreatePrButton, PlanChecksRunner, OperationBuilder |
| MED | `inventory_items.safety_stock`/`pack_size` have no CHECK → negative SS yields shortage=true/recommend_qty=0 (mask); negative pack under-orders | Migration **0067** (drafts: `safety_stock>=0`, `pack_size>0`; prod preflight = 0 violations) |
| MED | `palm_status_history` writable by any org member, no op.execute gate, no cross-org asset check | Migration **0068** (re-emit policy + op.execute + same-org asset) |
| LOW | advisor 0028/0029: anon/PUBLIC EXECUTE on `authorize`/`user_org_ids` | Migration **0069** (revoke from anon/PUBLIC, keep authenticated) |

> Migrations 0067–0069 are **drafts on the branch — NOT applied to prod.** Unlike 0049–0066 (which
> were already CI-green on main), these are new; they go through PR review + pgTAP CI, then the Owner
> applies. Preflight on prod confirms they apply cleanly.

### Tracked (need a tested PR or a product decision — not hot-patched)
| Sev | Finding | Where |
|---|---|---|
| CRIT→MED | **#270 C1** — `fn_post_receipt` keys received_qty by item_id not line id → phantom on_hand (or un-receivable PR) when a PR has duplicate same-item lines | issue #270 |
| MED | **#270 C2** — engine projects an overdue-vs-today PO as supply (`needed_by >= min(planned_at)`, not `>= current_date`) | issue #270 |
| HIGH | **Budget guardrail inert** — `budget_lines.committed`/`actual` never maintained (frozen seed); gate is also display-only; NULL est_cost counts as 0 | issue #157 |
| MED | **Grant regression** — default privileges re-grant anon/authenticated CRUD+TRUNCATE on post-0027 tables (people_compensation); contained only by FORCE RLS | issue #317 (new) |
| MED | **SQL↔TS engine parity drift** — `lib/stock-calc.ts recommendPurchase` is 2 fixes behind the RPC; "parity test" claim is false (latent — UI uses the RPC) | issue #161 |
| LOW-MED | `coverage/actions.ts:133` swallows the demandOp read error → a transient failure can drop a PO from the projection | (fold into #270/#199) |
| LOW | `fn_bin_rebuild` no authorize gate + no unit/pack normalization; `plans`/`people` writes org-only no role gate; assets/plan_operations unaudited; role cast in auth.ts; login demo missing 2 roles | backlog |

### Closed this session (verified fixed on prod 0066)
- **#306** — cross-org FK sweep complete (0063/0064/0065).
- **#280** — F2=0051, F4=0055, F5=0054 (F1 retracted).

## Recommended next sequence (one PR each, Owner-gated)
1. Merge this PR (landing + offline + migration drafts 0067–0069) → Owner applies 0067–0069 to prod.
2. **#270 C1 + C2** — engine fix PR with pgTAP (highest correctness priority).
3. **#157** — make budget figures table-derived + the gate enforce (HIGH; needs a product call on block-vs-warn).
4. **#317** — grant-layer lockdown + default-privileges + pgTAP grant invariant.
5. **#161** — TS/SQL parity (or delete the dead TS recommend path).
6. Backlog: LOW items + research issues #215–#226.
